import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  section: {
    marginTop: 10,
    marginBottom: 10,
  },
  error: {
    background: "blue",
    // color: "#f04040",
  },
}));

export default useStyles;
